
const COLORS = require("../../consts").COLORS;

module.exports = {
    command: function(msg) {
        msg.channel.send({
            "embed": {
                "title": "Help",
                "color": COLORS.BLUE,
                "description": `
                    [Príkazy](https://fajsiex.ml/docs/caj-bot.html)
                    [GitHub repo (Source)](https://github.com/FajsiEx/caj-ministrator)
                `
            }
        });
    }
}